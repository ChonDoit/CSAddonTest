name: Build

#Concurrency scoped to the specific branch/ref, not global
concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

on:
  workflow_dispatch:

  push:
    branches:
      - master
      - main
    paths-ignore:
      - '*.md'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v5
        with:
          path: "src"

      - name: Checkout Builds Branch
        uses: actions/checkout@v5
        with:
          ref: "builds"
          path: "builds"

      - name: Clean old builds
        run: |
            find . -name "*.cs3" -path "*/build/*" -delete || echo "No .cs3 files to delete"
            find . -name "*.jar" -path "*/build/*" -delete || echo "No .jar files to delete"
            find . -name "plugins.json" -path "*/build/*" -delete || echo "No plugins.json to delete"

      - name: Setup JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: "adopt"
          java-version: 17

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3.2.2

      - name: Access Secrets & Create Properties
        env:
          TMDB_KEY: ${{ secrets.TMDB_KEY }}
        run: |
          cd $GITHUB_WORKSPACE/src
          touch local.properties
          echo "SIMKL_API=$SIMKL_API" >> local.properties
          echo "TMDB_KEY=$TMDB_KEY" >> local.properties
          echo "CC_COOKIE=$CC_COOKIE" >> local.properties
          echo "CINE_API=$CINE_API" >> local.properties
          echo "CASTLE_API=$CASTLE_API" >> local.properties

      - name: Build Plugins
        run: |
          cd $GITHUB_WORKSPACE/src
          chmod +x gradlew
          ./gradlew clean
          ./gradlew make clean
          ./gradlew make makePluginsJson
          find . -name "*.cs3" -path "*/build/*" -exec cp {} $GITHUB_WORKSPACE/builds/ \; || echo "No .cs3 files found"
          find . -name "*.jar" -path "*/build/*" -exec cp {} $GITHUB_WORKSPACE/builds/ \; || echo "No .jar files found"
          find . -name "plugins.json" -path "*/build/*" -exec cp {} $GITHUB_WORKSPACE/builds/ \; || echo "No plugins.json found"

      - name: Push builds
        run: |
          cd $GITHUB_WORKSPACE/builds
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Actions"
          git add .

          git commit --amend -m "Build $GITHUB_SHA" || git commit -m "Build $GITHUB_SHA"
          git push --force